Code to train wav2vec2 
requires data from openslr
learning rate not configured, this will require a hyperparameter search and configuring for specific batch sizes
Setup so it is able to use an arbitary number of GPUs (via pytorch lightning)

